<UserControl x:Class="Org.Cen.RobotManager.Pages.RawDataControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="600">

    <Grid x:Name="MotionGrid">
        <DataGrid x:Name="MotionDataGrid" >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding PidTime}" Header="PidTime"/>
                <DataGridTextColumn Binding="{Binding NormalPosition}" Header="NormalPosition"/>
                <DataGridTextColumn Binding="{Binding NormalSpeed}" Header="NormalSpeed"/>
                <DataGridTextColumn Binding="{Binding Position}" Header="Position"/>
                <DataGridTextColumn Binding="{Binding Speed}" Header="Speed"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
